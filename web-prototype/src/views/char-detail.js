import { notBuilt } from './_notbuilt.js';

// STUB — char.detail. Replace this whole file with the real view.
export default async function view(args = {}) {
  return notBuilt('char-detail', args);
}
