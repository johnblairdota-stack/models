# The seeded canvas bundle: ~2 MB of editor payload, regenerated by seed-canvas.mjs from the
# .dc.html artboards beside it. The artboards are the source; this is the build output.
sound-desk.html
# Scratch renders from the _preview / _shot / _crop instruments.
_preview.html
*.png
